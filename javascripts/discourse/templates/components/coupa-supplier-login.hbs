{{#if shouldDisplay}}
  {{! check if correct route }}
  {{#if oidc}}
    {{! checks if oidc button exists }}
    {{d-button
      action=(action "externalLogin" oidc)
      translatedLabel=oidc.title
      icon="sign-in-alt"
      class="btn-primary"
    }}
  {{/if}}
{{/if}}